query GetIssues($milestone: [String]!, $groupName: ID!) {
  group(fullPath: $groupName) {
    issues(milestoneTitle: $milestone, assigneeUsernames: "malishevis") {
      nodes {
        ...IssueFields
      }
    }
  }
}
